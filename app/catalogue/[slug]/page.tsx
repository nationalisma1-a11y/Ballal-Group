import Image from "next/image";
import Link from "next/link";
import { notFound } from "next/navigation";
import { ArrowLeft, ArrowRight, MessageCircle } from "lucide-react";
import { whatsapp, PropertyCard } from "@/components/site";
import { inventory } from "@/lib/data";
export function generateStaticParams(){return [{slug:"villa-moderne"},{slug:"residence-neuilly"},{slug:"duplex-modernes"},{slug:"villa-familiale"},{slug:"immeuble-urbain"}]}
export default async function PropertyPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=inventory.find(x=>x.slug===slug);if(!p)notFound();const related=inventory.filter(x=>x.slug!==p.slug).slice(0,3);return <main><div className="container-shell py-8"><Link href="/catalogue" className="inline-flex items-center gap-2 text-sm font-bold text-slate-500 hover:text-orange"><ArrowLeft size={16}/>Retour au catalogue</Link><div className="mt-8 grid gap-10 lg:grid-cols-[1.1fr_.9fr] lg:items-center"><div className="relative aspect-[4/3] overflow-hidden rounded-3xl"><Image src={p.image} alt={p.title} fill priority className="object-cover"/></div><div><span className="rounded-full bg-green/10 px-3 py-1 text-xs font-bold uppercase tracking-wider text-green">{p.tag} · {p.type}</span><h1 className="mt-5 font-display text-4xl font-bold text-navy md:text-5xl">{p.title}</h1><p className="mt-3 text-lg text-slate-500">{p.location}</p><p className="mt-7 leading-7 text-slate-600">{p.description} Cette fiche présente un bien de démonstration. Contactez-nous pour connaître les disponibilités et recevoir une sélection adaptée à votre recherche.</p><div className="mt-8 flex flex-wrap gap-3"><a href={`${whatsapp}?text=${encodeURIComponent(`Bonjour Ballal, je souhaite visiter le bien : ${p.title}`)}`} target="_blank" rel="noreferrer" className="rounded-full bg-green px-6 py-3 font-bold text-white"><MessageCircle className="mr-2 inline" size={18}/>Demander une visite</a><Link href="/contact" className="rounded-full border border-navy px-6 py-3 font-bold text-navy">Nous contacter</Link></div></div></div></div><section className="bg-[#eef3ef] py-16"><div className="container-shell"><div className="mb-8 flex items-end justify-between"><h2 className="font-display text-3xl font-bold text-navy">Vous aimerez aussi</h2><Link href="/catalogue" className="font-bold text-orange">Tout voir <ArrowRight className="ml-1 inline" size={16}/></Link></div><div className="grid gap-6 md:grid-cols-3">{related.map(x=><PropertyCard key={x.slug} p={x}/>)}</div></div></section></main>}
