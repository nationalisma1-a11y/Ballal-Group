"use client";
import { useMemo, useState } from "react";
import { Search, SlidersHorizontal } from "lucide-react";
import { PropertyCard } from "@/components/site";
import { inventory } from "@/lib/data";
export default function Catalogue(){const [query,setQuery]=useState("");const [type,setType]=useState("Tous");const filtered=useMemo(()=>inventory.filter(p=>(type==="Tous"||p.type===type)&&`${p.title} ${p.location}`.toLowerCase().includes(query.toLowerCase())),[query,type]);return <main><section className="bg-navy-deep py-20 text-white"><div className="container-shell"><p className="mb-3 text-xs font-bold uppercase tracking-[.25em] text-orange">La collection Ballal</p><h1 className="font-display text-5xl font-bold">Nos biens</h1><p className="mt-5 max-w-xl text-slate-300">Une sélection de propriétés pour habiter, investir et construire la suite de votre histoire.</p></div></section><section className="container-shell py-14"><div className="mb-10 flex flex-col gap-4 rounded-2xl border border-slate-200 bg-white p-4 shadow-sm md:flex-row"><label className="flex flex-1 items-center gap-3 rounded-xl bg-slate-50 px-4"><Search size={18} className="text-slate-400"/><span className="sr-only">Rechercher</span><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Rechercher par nom ou quartier" className="w-full bg-transparent py-3 outline-none"/></label><div className="flex items-center gap-2"><SlidersHorizontal size={18} className="text-orange"/>{["Tous","Villa","Appartement","Maison","Immeuble"].map(t=><button key={t} onClick={()=>setType(t)} className={`rounded-full px-3 py-2 text-xs font-bold ${type===t?"bg-navy text-white":"bg-slate-100 text-slate-600"}`}>{t}</button>)}</div></div><p className="mb-5 text-sm text-slate-500">{filtered.length} résultat{filtered.length>1?"s":""} · Inventaire de démonstration</p>{filtered.length?<div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">{filtered.map(p=><PropertyCard key={p.slug} p={p}/>)}</div>:<div className="rounded-2xl border border-dashed border-slate-300 p-14 text-center text-slate-500">Aucun bien ne correspond à votre recherche.</div>}</section></main>}
